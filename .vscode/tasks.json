{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run the 'store' Rails server",
      "type": "shell",
      "command": "bin/rails server",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "isBackground": true,
      "problemMatcher": []
    }
  ]
}